import unknownArtistImage from '@/assets/images/unknown_artist.jpg'
import unknownTrackImage from '@/assets/images/unknown_track.jpg'
import { Image } from 'react-native'

export const unknownTrackImageUri = Image.resolveAssetSource(unknownTrackImage)
export const unknownArtistImageUri = Image.resolveAssetSource(unknownArtistImage)
